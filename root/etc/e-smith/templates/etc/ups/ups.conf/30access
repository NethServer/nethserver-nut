{
    $ups = $nut-server{'Ups'} || 'UPS';
    $driver = $nut-server{'Model'} || '';
    $device = $nut-server{'Device'} || '';
    
    $OUT.="[$ups]\n";
    $OUT.="driver = $driver\n";
    $OUT.="port = $device";
}
